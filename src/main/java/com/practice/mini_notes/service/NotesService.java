/*
package com.practice.mini_notes.service;

import com.practice.mini_notes.model.Note;
import com.practice.mini_notes.repository.NotesRepo;

import java.time.LocalDateTime;


public class NotesService {
//    NotesRepo notesRepo;
//    public Note createNote(Note note) {
//        note.setCreatedAt(LocalDateTime.now());
//        note.setUpdatedAt(LocalDateTime.now());
//        return notesRepo.save(note);
//    }
//
//    public Note updateNote(String id, Note updatedData) {
//        Note existingNote = notesRepo.findById(id).orElseThrow(() -> new RuntimeException("Note not found"));
//        existingNote.setTitle(updatedData.getTitle());
//        existingNote.setContent(updatedData.getContent());
//        existingNote.setUpdatedAt(LocalDateTime.now());
//        return notesRepo.save(existingNote);
//    }
}
*/
